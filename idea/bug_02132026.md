COMPLETED - 1. AI model setting did not apply to the agentic search app; also returns this error. 
(Fixed by unifying AI calls to use dashboard.services.ai_handler which respects user session and uses correct .env settings)

COMPLETED - 1.1 make sure that AI settings was successfully passed to all apps, including agentic search, dashboard, labelcomp, and drugtox.
(Verified and updated Search, Dashboard, and Labelcomp. Drugtox has no AI features but its header was updated for consistency.)

COMPLETED - 2. the height of Official FDALabel badge in the internal env is different from others. we can remove this "Access internal search interfaces for FDALabel and CDER-CBER versions." to adapt to same height;

COMPLETED - 3. afd.db is still synced even added to ignore. we should remove it from the .git history to stop updating this database across different dev sites.
(Removed from git cache via `git rm --cached`)

COMPLETED - 4. re-design the agentic search homepage, including: (1)no user related functions like ai config, log out; (2) remove the drug-snippet icon. (3)revise the welcome page to clarify the specific tool of this app, instead of the whole askFDALabel (suite)

COMPLETED - 5. change the labelcomp, snippets, and agentic search overall layout, to be consistent with the suite homepage, dashboard and drugtox
(All headers now use the .header-main styling and consistent HTML structure)

COMPLETED - 6. implement to full function of the user dropdown list: only keep the ai config and add change password above log out. we will mostly manage the projects and favorites in the dashboard.
(Added Change Password route and template; updated user dropdown menu)

COMPLETED - 7. there are two sets of FDALabel env defined:
(Unified to use FDALabel_SERV, FDALabel_PORT, etc. across the dashboard config)
